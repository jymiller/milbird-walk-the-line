# site/ — the deployable surface

Static. No build step. Point EdgeOne Makers at this folder:

    Root directory       site
    Build output         (leave empty)
    Build command        (leave empty)
    Install command      (leave empty)
    Preset framework     Other

Contents:
    index.html          the locate inventory — map + 53 cards with embedded frames
    api/locates.json    GeoJSON FeatureCollection, 53 detections
    api/summary.json    tallies by colour and utility class, plus provenance
    api/stage2.json     the proposed Stage 2 production record
